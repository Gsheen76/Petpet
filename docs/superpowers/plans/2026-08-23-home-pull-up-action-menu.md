# 家园右下角上拉操作菜单实施记录

## 范围

- 修改 `petpet/home/window.py` 的操作按钮几何、绘制与点击状态机。
- 修改 `tests/test_home_scene.py`，用右下角上拉菜单契约替换旧顶部横排按钮契约。
- 不修改商店、互动、装修、退出的业务入口。

## 已完成

- [x] 新增右下角常驻主按钮与向上展开的四项主菜单。
- [x] 将退出放在最上方，商店放在最靠近主按钮的位置。
- [x] 将互动四项改为同位置的二级上拉菜单，并支持返回。
- [x] 点击菜单外空白时关闭并消费点击。
- [x] 使用暖色渐变、统一圆角、描边和投影优化按钮。
- [x] 保留未展开选项的非命中状态，避免隐藏区域阻止小狗移动。
- [x] 家园专项测试通过：`74 passed`。
- [x] 全量自动化测试：`657 passed`；字节码编译与空白差异检查通过。
- [x] 源码 GUI 已精确重启，确认唯一运行进程为 `pythonw.exe`，入口为 `D:\Agent_project\Petpet\pet.py`。
